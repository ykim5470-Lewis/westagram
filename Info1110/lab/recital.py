a = [5, 2 ,3 ,4]
a.append(True)
print(a)
a.insert(1,1.0)
# insert at the location where index[1], put 1.0
del a[0]
#delete list a[0] 
list('string')
#['s' 't' 'r' 'i' 'n' 'g'] represented

#Fiel I/O Input & Output    ~.py ~.txt ~doc If you don't interpret in terms of each file format, non-sense
#So we need to use palin txt 
#'/n' new line '/t' tap